# Local overrides (example)

Copy this file to `CLAUDE.local.md` (gitignored) for machine- or session-specific notes that
should NOT be shared with the team or committed. Claude Code reads `CLAUDE.local.md` automatically.

Keep it short. This is for *local truth*, not project policy — policy goes in `CLAUDE.md` / `docs/`.

## Current focus
<!-- TODO: what you're working on right now, so a fresh session has context. -->
- Active spec: docs/specs/<name>/
- Branch: <branch>

## Local environment
<!-- TODO: anything specific to this machine. -->
- Run app: `<command>`
- Run tests: `<command>`
- Services to start first: `<command>`

## Execution mode (see docs/workflows/sandbox-policy.md)
- Default mode for this checkout: local-safe | focused-autonomous | high-risk
- Isolation in use: none | bash-sandbox | container | VM

## Temporary notes / reminders
<!-- Scratch space. Delete when stale. -->
-
